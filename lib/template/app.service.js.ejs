const Service = require('@reinjs/rein-class');

module.exports = class <%= cname %> extends Service {
  constructor(...args) {
    super(...args);
  }

  <% if (init) { %>
  async hello() {
    return 'Hello World!';
  }
  <% } %>
}