const Worker = require('<%- framework %>');

const worker = new Worker({
  cwd: __dirname
});

worker.listen()
  .then(() => worker.logger.info('server listened success.'))
  .catch(e => worker.logger.error(e));