const Controller = require('@reinjs/rein-class');

module.exports = class <%= cname %> extends Controller {
  constructor(...args) {
    super(...args);
  }

  <% if (init) { %>
  async hello() {
    this.ctx.body = await this.service.index.hello();
  }
  <% } %>
}